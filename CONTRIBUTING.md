* When editing text files, make sure you use tabs, not spaces ! (One exception are the FDM file and JSBSim systems, where this is not permitted (?))
* For new liveries, always include the SVG file (if you created one).
